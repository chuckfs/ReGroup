/**
 * Friend presentation primitives shared across the app. Feature-specific
 * friend UI (hero card, action stack, metric row) lives in
 * `/features/friends/components`.
 */
export { FriendRow } from './FriendRow';
