/**
 * Bottom-sheet primitives shared across features. The full draggable
 * sheets themselves live in their feature folders (e.g. the
 * `GroupSheet` is in `/features/group/components`); only generic
 * sheet-building parts belong here.
 */
export { BottomSheetHandle } from './BottomSheetHandle';
