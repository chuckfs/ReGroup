/**
 * Onboarding feature module.
 *
 * Today: just the splash. Reserved for future intro carousel, permissions
 * walkthrough (location, notifications), and account creation flow.
 *
 * The root `app/index.tsx` route renders `SplashScreen` and replaces to
 * the home tab when ready.
 */
export { default as SplashScreen } from './screens/SplashScreen';
